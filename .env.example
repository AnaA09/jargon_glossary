# Optional: add an OpenRouter API key to use AI extraction.
# Without it, the app runs in a local demo mode for common procurement terms.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/auto
APP_URL=http://localhost:8000
